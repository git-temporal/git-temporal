import * as React from 'react';
import reactDom from 'react-dom';

const element: HTMLElement = document.getElementById('gitTemporal');

reactDom.render(<h1>We are motherfucking here!</h1>, element);
